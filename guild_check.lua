local guild_check_addon = {
	name = "Guild Check",
	author = "Eludelu",
	version = "1.0",
	desc = "Guild Check feature"
}

local function OnLoad()
    -- Nothing to load for now
end

local function OnUnload()
    -- Nothing to unload for now
end

guild_check_addon.OnLoad = OnLoad
guild_check_addon.OnUnload = OnUnload

return guild_check_addon
